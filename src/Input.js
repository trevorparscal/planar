/**
 * Input.
 *
 * @class
 */
Planar.Input = class {
	/**
	 * Create an input.
	 *
	 * @constructor
	 * @param {Element} element DOM element to listen to events on
	 */
	constructor( element ) {
		this.element = element;
	}
};
